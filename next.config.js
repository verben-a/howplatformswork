/** @type {import('next').NextConfig} */
const nextConfig = {
  // ─── GitHub Pages (static export) ────────────────────────────────────────
  // Uncomment the two lines below ONLY when deploying to GitHub Pages.
  // Leave them commented when deploying to Vercel (Vercel handles this automatically).
  //
  // output: 'export',
  // images: { unoptimized: true },
};

module.exports = nextConfig;
